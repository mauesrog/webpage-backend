import ProjectData from '../data/project_data';
import InternshipData from '../data/internship_data';

// create a schema for posts with a field
const Data = InternshipData.concat(ProjectData);

export default Data;
